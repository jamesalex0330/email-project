import bcrypt from "bcryptjs";
import models from "../models";
import service from "../services/mailer";

const { Sequelize } = models.sequelize;

export default {
  async getAttachment(req) {
    try {
     
    } catch (error) {
      throw Error(error);
    }
  },
  
}